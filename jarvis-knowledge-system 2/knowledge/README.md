# JARVIS Knowledge System

Place JSON knowledge files in this directory. Each JSON file should have keys as
knowledge topics and values as descriptive text.

Example:
```json
{
  "albert einstein": "Albert Einstein...",
  "gravity": "Gravity is..."
}
```
